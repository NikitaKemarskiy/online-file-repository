$(document).ready(function() {

	// Variables
	const socket = io(); // Socket initialization
	let current_path = $('.header-email').eq(0).text(); // Current storage path

	// Functions

	// Socket.io
});